# List of currently supported object types:
#	ascension_perk
#	authority
#	authority_swaps
#	building
#	civic (includes origins)
#	district
#	edict
#	ethic
#	starbase_building
#	starbase_module
#	technology (note: the tooltip currently only shows the name and description)
#	tradition
#	tradition_category
#	trait (both species and leader traits)

# In GUI, instantTextBoxType has a "concepts_show_missing_dlc" parameter. If set to "yes", database concept tooltips inside the text box will show unmet DLC requirements for the database object.
# Currently, this only works for authorities, civics, ethics and species traits.

database_object_types = {
	ascension_perk = {
		type = ascension_perk
		swap_type = swapped_ascension_perk
	}
	authority = {
		type = authority
		swap_type = swapped_authority
	}
	building = {
		type = building
	}
	civic = {
		type = civic_or_origin
		swap_type = swapped_civic
	}
	district = {
		type = district
	}
	edict = {
		type = edict
	}
	ethic = {
		type = ethic
	}
	starbase_building = {
		type = starbase_building
	}
	starbase_module = {
		type = starbase_module
	}
	technology = {
		type = technology
		swap_type = swapped_technology
	}
	tradition = {
		type = tradition
		swap_type = swapped_tradition
	}
	tradition_category = {
		type = tradition_category
	}
	trait = {
		type = trait.species_trait
	}
	leader_trait = {
		type = trait.leader_trait
	}
	cosmic_storm = {
		type = storm_types
	}
	modifier = {
	    type = static_modifier
	}
	job = {
		localisation = job_
	}
    scripted_action = {
        type = scripted_action
    }

    # NOTE this database object type is undocumented but actually used in vanilla files
    policy = {
        # `policy:$POLICY$:$OPTION$` - where:
        # - `$POLICY$` is a definition with type `policy`
        # - `$OPTION$` is optional and may be a localisation which refer to a policy option
        # - TODO [chronicle 3.0.0+] but "type + localisation" form is not supported atm
        type = policy
    }
}
