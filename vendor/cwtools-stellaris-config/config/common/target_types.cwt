types = {
    type[target_type] = {
        path = "game/common/target_types"
        path_extension = .txt
        localisation = {
            name = "$"
        }
    }
}

target_type = {
    target_type = enum[target_type]
}
