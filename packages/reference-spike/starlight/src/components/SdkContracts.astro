---
/**
 * The hand-written SDK contracts. Static: nothing here has state.
 */
import { SdkContracts } from "../../../src/app/components/sdk-contracts.tsx";
import { buildOf } from "../builds.ts";

interface Props {
  page: string;
}

const { page } = Astro.props;
---

<div class="not-content">
  <SdkContracts build={buildOf(page)} />
</div>
