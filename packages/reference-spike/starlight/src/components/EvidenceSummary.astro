---
/**
 * What supports this page, at the foot of it. Static.
 */
import { EvidenceSummary } from "../../../src/app/components/evidence-summary.tsx";
import { buildOf } from "../builds.ts";

interface Props {
  page: string;
}

const { page } = Astro.props;
---

<div class="not-content">
  <EvidenceSummary build={buildOf(page)} />
</div>
