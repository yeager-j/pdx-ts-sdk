// Generated by @pdx-ts/codegen-cwt. Do not edit.
// Source: cwtools-stellaris-config @ 37563eee6a40
// From: common/ascension_perk_categories.cwt

import type { LocalizationRef, LocalizedText } from "../authoring/localization.ts";
import type { DefinedContent } from "../content/authoring.ts";
import type { ContentField, ContentLocalisation } from "../content/schema.ts";
import type { ParsedAscensionPerkCategory } from "../installation/vanilla/parsed-definitions.ts";
import type {
  ContentPatchItem,
  PatchedContent,
  PatchInput,
} from "../installation/vanilla/patch.ts";
import type { AscensionPerkRef } from "./refs.ts";

/**
 * An ascension_perk_category, as the game's rules describe it.
 * Generated from `type[ascension_perk_category]` at `game/common/ascension_perk_categories`.
 */
export interface AscensionPerkCategoryFields {
  /**
   * Display text emitted to localization under `<id>`.
   * A bare string is the English shorthand.
   */
  name?: LocalizedText;
  /**
   * Display text emitted to localization under `<id>_desc`.
   * A bare string is the English shorthand.
   */
  desc?: LocalizedText;
  /** Ascension perks that belong to this category; emitted list order is preserved. */
  ascensionPerks: (AscensionPerkRef | string)[];
}

/** An ascension_perk_category with the id it is defined under. */
export interface AscensionPerkCategoryDef<
  Id extends string = string,
> extends AscensionPerkCategoryFields {
  /** Full content id, including the mod prefix. */
  id: Id;
}

/**
 * The localization keys one `ascension_perk_category` mints, as references.
 * Every slot is present whether or not the definition supplied its text:
 * the key follows from the id alone.
 */
export type AscensionPerkCategoryLoc = {
  /** The `<id>` key. */
  readonly name: LocalizationRef;
  /** The `<id>_desc` key. */
  readonly desc: LocalizationRef;
};

/** An ascension_perk_category registered with a mod, usable as a typed cross-reference. */
export type DefinedAscensionPerkCategory<Id extends string = string> = DefinedContent<
  "ascension_perk_category",
  AscensionPerkCategoryDef<Id>
>;

/**
 * What a patch of a vanilla ascension_perk_category may change.
 * Closed, so a typo is a compile error, and `id`-less: a patched definition
 * keeps vanilla's identity, because the override has to target the vanilla
 * key to win.
 */
export interface AscensionPerkCategoryPatch {
  /**
   * Replacement text for vanilla's own `<vanilla id>` key.
   * Emitted to `localisation/replace/`, the layer the game resolves ahead
   * of the ordinary one — a rename, not a new key. English is always
   * supplied, so a rename replaces the original English text too.
   */
  readonly name?: LocalizedText;
  /**
   * Replacement text for vanilla's own `<vanilla id>_desc` key.
   * Emitted to `localisation/replace/`, the layer the game resolves ahead
   * of the ordinary one — a rename, not a new key. English is always
   * supplied, so a rename replaces the original English text too.
   */
  readonly desc?: LocalizedText;
  /** Ascension perks that belong to this category; emitted list order is preserved. */
  readonly ascensionPerks?: PatchInput<(AscensionPerkRef | string)[]>;
}

/** A patched vanilla ascension_perk_category, ready for the win engine. */
export type PatchedAscensionPerkCategory = PatchedContent<ParsedAscensionPerkCategory>;

/** A patched vanilla ascension_perk_category placed into a capability feature. */
export type AscensionPerkCategoryPatchItem = ContentPatchItem<ParsedAscensionPerkCategory>;

/** How the writer lowers each member of {@link AscensionPerkCategoryFields} to PDXScript. */
export const ASCENSION_PERK_CATEGORY_FIELDS: readonly ContentField[] = [
  {
    key: "ascension_perks",
    member: "ascensionPerks",
    shape: "valueList",
    form: "list",
    conversion: "ref",
    refTypes: ["ascension_perk"],
  },
];

/** The localization slots an ascension_perk_category defines, with the key pattern each one mints. */
export const ASCENSION_PERK_CATEGORY_LOCALISATION: readonly ContentLocalisation[] = [
  { member: "name", pattern: "$", required: false },
  { member: "desc", pattern: "$_desc", required: false },
];
