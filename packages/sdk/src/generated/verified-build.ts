// Generated by @pdx-ts/codegen-vanilla from packages/codegen-vanilla/src/verified-build.ts. Do not edit.
// The override-rule oracle runs and the committed @pdx-ts/stellaris-ids generation were verified against Stellaris Pegasus 4.4.6.

/** The Stellaris build this SDK's evidence was gathered against. */
export const VERIFIED_STELLARIS_BUILD = "4.4.6";
/** The descriptor `supported_version` derived from {@link VERIFIED_STELLARIS_BUILD}. */
export const VERIFIED_SUPPORTED_VERSION = "v4.4.*";
/** What was verified against that build, in the words the evidence was recorded in. */
export const VERIFIED_BUILD_PROVENANCE =
  "The override-rule oracle runs and the committed @pdx-ts/stellaris-ids generation were verified against Stellaris Pegasus 4.4.6.";
/** The path a candidate directory must contain to be a Stellaris install. */
export const INSTALL_SENTINEL_PATH_SEGMENTS = ["common", "technology"] as const;
/** The install file the launcher writes the game version into. */
export const LAUNCHER_SETTINGS_FILENAME = "launcher-settings.json";
/** The field in that file holding the version. */
export const LAUNCHER_VERSION_FIELD = "rawVersion";
/** The prefix a descriptor `supported_version` carries. */
export const LAUNCHER_VERSION_PREFIX = "v";
/** Matches a bare `major.minor.patch` game version. */
export const CORE_GAME_VERSION_PATTERN = new RegExp("^\\d+\\.\\d+\\.\\d+$");
/** Captures the major and minor parts of a game version, for the `supported_version` it implies. */
export const SUPPORTED_VERSION_FROM_GAME_PATTERN = new RegExp("^v?(\\d+)\\.(\\d+)\\.");
/** Matches a descriptor `supported_version`, wildcards included. */
export const SUPPORTED_VERSION_PATTERN = new RegExp("^v?(\\d+|\\*)(\\.(\\d+|\\*)){0,2}$");
/** Where each platform installs Stellaris, for locating it without a configured path. */
export const PLATFORM_INSTALL_DEFAULTS = {
  darwin: [
    {
      kind: "home",
      segments: ["Library", "Application Support", "Steam", "steamapps", "common", "Stellaris"],
    },
  ],
  win32: [
    {
      kind: "absolute",
      path: "C:\\Program Files (x86)\\Steam\\steamapps\\common\\Stellaris",
    },
  ],
  other: [
    {
      kind: "home",
      segments: [".local", "share", "Steam", "steamapps", "common", "Stellaris"],
    },
    {
      kind: "home",
      segments: [".steam", "steam", "steamapps", "common", "Stellaris"],
    },
  ],
} as const;
