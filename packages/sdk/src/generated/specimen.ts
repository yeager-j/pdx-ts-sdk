// Generated by @pdx-ts/codegen-cwt. Do not edit.
// Source: cwtools-stellaris-config @ d68adb90f4f0
// From: common/specimens.cwt

import type { LocalizationRef, LocalizedText } from "../authoring/localization.ts";
import type { DefinedContent } from "../content/authoring.ts";
import type { ContentField, ContentLocalisation } from "../content/schema.ts";
import type { EconomicResourceBlock, TriggeredModifier } from "../content/types.ts";
import type { SpecimensRarity, SpecimenType } from "./enums.ts";
import type { SpriteRef } from "./refs.ts";
import type { ScopeName } from "./scopes.ts";

/**
 * A specimen, as the game's rules describe it.
 * Generated from `type[specimen]` at `game/common/specimens`.
 */
export interface SpecimenFields {
  /**
   * Display text emitted to localization under `<id>`.
   * A bare string is the English shorthand.
   */
  name?: LocalizedText;
  /**
   * Display text emitted to localization under `<id>_desc_short`.
   * A bare string is the English shorthand.
   */
  descShort?: LocalizedText;
  /**
   * Display text emitted to localization under `<id>_desc_details`.
   * A bare string is the English shorthand.
   */
  descDetails?: LocalizedText;
  icon: SpriteRef | string;
  type: SpecimenType;
  rarity: SpecimensRarity;
  isTradable?: boolean;
  isSellable?: boolean;
  resources?: EconomicResourceBlock<ScopeName>[];
  triggeredCountryModifier?: TriggeredModifier<"country">[];
}

/** A specimen with the id it is defined under. */
export interface SpecimenDef<Id extends string = string> extends SpecimenFields {
  /** Full content id, including the mod prefix. */
  id: Id;
}

/**
 * The localization keys one `specimen` mints, as references.
 * Every slot is present whether or not the definition supplied its text:
 * the key follows from the id alone.
 */
export type SpecimenLoc = {
  /** The `<id>` key. */
  readonly name: LocalizationRef;
  /** The `<id>_desc_short` key. */
  readonly descShort: LocalizationRef;
  /** The `<id>_desc_details` key. */
  readonly descDetails: LocalizationRef;
};

/** A specimen registered with a mod, usable as a typed cross-reference. */
export type DefinedSpecimen<Id extends string = string> = DefinedContent<
  "specimen",
  SpecimenDef<Id>
>;

/** How the writer lowers each member of {@link SpecimenFields} to PDXScript. */
export const SPECIMEN_FIELDS: readonly ContentField[] = [
  {
    key: "icon",
    member: "icon",
    shape: "value",
    form: "scalar",
    conversion: "ref",
    refTypes: ["sprite"],
  },
  { key: "type", member: "type", shape: "value", form: "scalar", conversion: "identity" },
  { key: "rarity", member: "rarity", shape: "value", form: "scalar", conversion: "identity" },
  {
    key: "is_tradable",
    member: "isTradable",
    shape: "value",
    form: "scalar",
    conversion: "identity",
  },
  {
    key: "is_sellable",
    member: "isSellable",
    shape: "value",
    form: "scalar",
    conversion: "identity",
  },
  {
    key: "resources",
    member: "resources",
    shape: "economicResources",
    form: "list",
    repeated: true,
  },
  {
    key: "triggered_country_modifier",
    member: "triggeredCountryModifier",
    shape: "triggeredModifierBlock",
    form: "list",
    repeated: true,
  },
];

/** The localization slots a specimen defines, with the key pattern each one mints. */
export const SPECIMEN_LOCALISATION: readonly ContentLocalisation[] = [
  { member: "name", pattern: "$", required: false },
  { member: "descShort", pattern: "$_desc_short", required: false },
  { member: "descDetails", pattern: "$_desc_details", required: false },
];
