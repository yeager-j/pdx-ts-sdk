// Generated by @pdx-ts/codegen-cwt. Do not edit.
// Source: cwtools-stellaris-config @ a4ee61a099b8
// From: alias[...] categories across the rule files

import type { ContentField } from "../content/schema.ts";
import { GOVERNMENT_TRIGGER_FIELDS } from "./government-trigger.ts";
import { MOON_INITIALIZER_FIELDS } from "./moon-initializer.ts";
import { PLANET_INITIALIZER_FIELDS } from "./planet-initializer.ts";

/** The name of an alias category the SDK can write. */
export type AliasStructCategory = "government_trigger" | "moon_initializer" | "planet_initializer";

/**
 * Every alias category's field table, keyed by the name its fields carry.
 * An `aliasStruct` field names its category instead of holding the table,
 * because a category contains itself; this is where the name resolves.
 */
export const ALIAS_STRUCT_CATALOG: Readonly<Record<AliasStructCategory, readonly ContentField[]>> =
  {
    government_trigger: GOVERNMENT_TRIGGER_FIELDS,
    moon_initializer: MOON_INITIALIZER_FIELDS,
    planet_initializer: PLANET_INITIALIZER_FIELDS,
  };

/**
 * Returns the field table an alias category name resolves to.
 * Throws when no generated category carries the name.
 */
export function aliasStructFieldsOf(category: string): readonly ContentField[] {
  const fields: readonly ContentField[] | undefined =
    ALIAS_STRUCT_CATALOG[category as AliasStructCategory];
  if (fields === undefined) {
    throw new Error(`No field table for alias category "${category}"`);
  }
  return fields;
}
