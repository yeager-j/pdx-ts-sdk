namespace = hello_galaxy_resonance

country_event = {
	id = hello_galaxy_resonance.1
	title = hello_galaxy_resonance.1.name
	desc = hello_galaxy_resonance.1.desc
	is_triggered_only = yes
	immediate = {
		random_list = {
			60 = {
				set_country_flag = hello_galaxy_heard_the_hum
				give_tech_option_or_progress_effect = {
					TECH = hello_galaxy_tech_resonance_theory
				}
			}
			40 = {
				modifier = {
					factor = 2
					is_at_war = yes
				}
				modifier = {
					factor = 3
					has_actual_deficit = {
						RESOURCE = minerals
					}
				}
				every_owned_planet = {
					limit = {
						has_owner = yes
					}
					save_event_target_as = hello_galaxy_storm_world
					planet_event = {
						id = hello_galaxy_resonance.2
						days = 30
					}
				}
			}
		}
		if = {
			limit = {
				has_country_flag = hello_galaxy_heard_the_hum
			}
			event_target:hello_galaxy_storm_world = {
				add_deposit = d_minerals_1
			}
		}
		else = {
			log = "the hum went unheard"
		}
	}
	option = {
		name = hello_galaxy_resonance.1.a
	}
}

planet_event = {
	id = hello_galaxy_resonance.2
	title = hello_galaxy_resonance.2.name
	desc = hello_galaxy_resonance.2.desc
	is_triggered_only = yes
	immediate = {
		from = {
			add_resource = {
				influence = 50
			}
		}
	}
	option = {
		name = hello_galaxy_resonance.2.a
	}
}
