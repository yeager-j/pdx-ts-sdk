out| would write <project>/src/content/resonance_theory.ts
out| /**
out|  * One technology, and the feature that places it.
out|  *
out|  * Generated once by the `technology` recipe, and yours from here. Nothing reads
out|  * this file back: there is no marker in it, no version, and no upgrade — so
out|  * rename it, add to it, or delete it as the mod grows.
out|  *
out|  * `#mod` is the project's own alias for `src/mod.ts` (see `package.json#imports`),
out|  * so moving this file deeper inside the content directory never rewrites the
out|  * import. The filename decides nothing either: the `mod.feature(...)` call at the
out|  * bottom is what names the emitted files.
out|  */
out|
out| import { mod } from "#mod";
out|
out| export const resonanceTheory = mod.technology("resonance_theory", {
out|   name: "Resonance Theory",
out|   desc: "PLACEHOLDER: what researching this unlocks, in a sentence or two.",
out|   area: "physics",
out|   category: "particles",
out|   tier: 2,
out|   cost: 2000,
out|
out|   // Technologies that must be researched first. A vanilla technology is a plain
out|   // string, taken as given — nothing checks a bare literal;
out|   // `vanilla.technology(...)` from @pdx-ts/sdk is the checked form, against the
out|   // real id set when @pdx-ts/stellaris-ids is installed. One of your own is the
out|   // binding another feature file exports, imported as usual.
out|   // prerequisites: ["tech_basic_science_lab_1"],
out|
out|   // How likely the game is to offer this as a research option next to the
out|   // others in its category. 100 is the conventional starting weight.
out|   // weight: 100,
out| });
out|
out| export const feature = mod.feature("resonance_theory", [resonanceTheory]);
err| A real run would refuse: <project>/src/content/resonance_theory.ts already exists, and nothing here replaces a file that is already yours. Generate under a different name, or move the existing file first.
exit 0
