# Ship scope

Ship scope represents a ship while the SDK records script against that ship.

## Common entry points

Common entry points include `ship_event` bodies and the generated `ship` and `lastCreatedShip` scope
links. These are examples, not an exhaustive list of game paths.
