# Pop faction scope

Pop faction scope represents a population faction through the generated SDK script interface.

## Common entry points

Common entry points include `pop_faction_event` bodies and the generated `popFaction` and
`lastCreatedPopFaction` scope links. These are examples, not an exhaustive list of game paths.
