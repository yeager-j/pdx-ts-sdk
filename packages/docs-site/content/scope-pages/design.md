# Design scope

Design scope represents a ship design through the generated SDK script interface.

## Common entry points

Common entry points include the generated `design` and `lastCreatedDesign` scope links. These are
examples, not an exhaustive list of game paths.
