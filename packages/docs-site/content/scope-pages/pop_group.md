# Pop group scope

Pop group scope represents a population group through the generated SDK script interface.

## Common entry points

Common entry points include `pop_group_event` bodies and generated links such as `popGroup` and
`unhappiestPop`. These are examples, not an exhaustive list of game paths.
