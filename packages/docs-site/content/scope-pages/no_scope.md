# No-scope context

No-scope is the generated SDK context for script that intentionally has no game-object scope.

## Common entry points

Common entry points include the generated `noScope` transition and typed callbacks whose contract
uses `NoScopeScope`. These are examples, not an exhaustive list of script paths.
