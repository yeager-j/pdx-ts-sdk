# Sector scope

Sector scope represents a sector while the SDK records script against that sector.

## Common entry points

Common entry points include the generated `sector` scope link and typed callbacks that supply a
`SectorScope`. These are examples, not an exhaustive list of game paths.
