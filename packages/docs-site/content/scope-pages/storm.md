# Storm scope

Storm scope represents a cosmic storm through the generated SDK script interface.

## Common entry points

Common entry points include `cosmic_storm_event` bodies and the generated `lastCreatedCosmicStorm`
scope link. These are examples, not an exhaustive list of game paths.
