# Species scope

Species scope represents a species while the SDK records script against that species.

## Common entry points

Common entry points include generated links such as `ownerSpecies`, `ownerMainSpecies`,
`lastCreatedSpecies`, and `growingSpecies`. These are examples, not an exhaustive list of game paths.
