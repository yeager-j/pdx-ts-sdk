# Deposit scope

Deposit scope represents a deposit while the SDK records script against that deposit.

## Common entry points

Common entry points include deposit-creation callbacks and the generated `lastAddedDeposit` scope
link. These are examples, not an exhaustive list of game paths.
