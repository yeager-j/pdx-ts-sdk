# Mission scope

Mission scope represents a mission value through the generated SDK script interface.

## Common entry points

Enter this scope through typed callbacks or script contracts that supply a `MissionScope`. The
generated transition table below records the outgoing links available from that context.
