---
title: Static Modifiers
description: Bundle concrete modifier values into named effects that game content can apply and remove.
registries: [static_modifier]
---

import { Callout } from "fumadocs-ui/components/callout";

import { FieldTable } from "@/components/FieldTable";
import { PairedExample } from "@/components/PairedExample";

A **static modifier** is a named bundle of concrete modifier values. Events,
situations, planets, and other game systems can apply or remove that bundle as
one unit. The SDK writes definitions under `common/static_modifiers/`.

See [Economic modifiers](/concepts/economic-modifiers/) for how a static bundle
can supply values to generated economic keys and scripted-modifier keys.

## Declare one host scope

Every SDK-authored static modifier requires `hostScope`. This is the kind of
game object that may hold the modifier, not every object whose statistics the
modifier can affect. The declaration emits no PDXScript.

For example, a country-hosted modifier can contain
`planet_jobs_engineering_research_produces_mult`. Stellaris propagates that
economic row from the country to its planets. The propagation does not make a
planet a second valid host. Define a separate modifier when the same values
must be hosted on a different kind of object.

The returned item carries this declaration. Executing consumers reject an
authored item used from a different scope. These include `addModifier`,
`removeModifier`, `hasModifier`, their stage-modifier variants, and
`exportModifierDurationToVariable`. A raw string or a plain vanilla or
third-party reference has no authored witness, so those forms remain explicit
unchecked fallbacks.

## Write modifier values directly into the definition

The `modifiers` callback uses the SDK's modifier builder. Its rows are written
at the root of the static-modifier block:

<PairedExample name="static-modifiers" />

There is no `modifier = { ... }` or `modifiers = { ... }` wrapper in the
emitted PDXScript. Stellaris expects keys such as
`country_unity_produces_mult` directly beside the definition's metadata. Use
the returned item wherever an SDK field accepts a static-modifier reference.

The callback is narrowed by `hostScope`. It accepts rows valid for that host,
including economic rows that the game propagates to owned objects.

## Preview without applying

Use `scope.previewModifier(modifier)` inside an effect closure when a tooltip
must display a modifier that is actually applied elsewhere. It emits
`tooltip = { add_modifier = { modifier = ... } }` as a non-executing preview.
The surrounding scope is not treated as the modifier's host.

## Display metadata must match the application path

`name` is required and `desc` is optional. The SDK emits them as localization
for the minted modifier id and its `_desc` key.

`iconFrame` belongs to the planet-modifier form in the game rules. `icon`,
`important`, `customTooltip`, and `showOnlyCustomTooltip` are general fields;
vanilla frequently uses the icon and tooltip fields for planet modifiers. The
generated interface cannot prove that the receiving game system displays them,
and the SDK does not check that an icon file exists.

Use `hideFromCountryList: true` when a modifier should not appear in the
country government modifier list. It does not hide the modifier from every
other interface.

## Cross-planet propagation needs a paired definition

`applyModifierToOtherPlanets` points to a second static modifier. Vanilla's
`penal_colony` uses it to apply a crime reduction to the empire's other
planets. The source comment states that this link works only for timed planet
modifiers, while the decision that applies `penal_colony` uses `days = -1`.
Treat the lifetime requirement as game-specific behavior to verify.

Define both modifiers, pass the returned target item to
`applyModifierToOtherPlanets`, and test application, expiration, owner changes,
planet creation, and save/load. A checked reference confirms the target id, but
the SDK does not enforce any lifetime requirement.

<Callout type="warn" title="Host scope is not full game simulation">
  The host witness checks authored executing call sites. Stellaris still decides duration, stacking,
  propagation, display, and removal behavior. Test that complete path in the game.
</Callout>

See [Testing your mod](/guides/testing-your-mod/) for the live-game checks that
the build cannot perform.

## API and vanilla references

- [`StaticModifierDef`](https://github.com/yeager-j/pdx-ts-sdk/blob/main/packages/sdk/src/generated/static-modifier.ts)
  is the generated definition used by `mod.staticModifier`.
- `StaticModifierScope` names the supported host scopes, derived from the
  scopes where the game exposes `add_modifier`.
- [`vanilla.staticModifier`](https://github.com/yeager-j/pdx-ts-sdk/blob/main/packages/sdk/src/generated/vanilla-refs.ts)
  provides checked references to vanilla static-modifier ids.

## Field reference

<FieldTable registry="static_modifier" />
