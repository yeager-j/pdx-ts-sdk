---
title: Country scope
description: Generated methods, event kinds, and transitions for CountryScope.
sidebar:
  order: 3
---

import EventFireMethods from "../../../components/EventFireMethods.astro";
import EventKinds from "../../../components/EventKinds.astro";
import ScopeEffects from "../../../components/ScopeEffects.astro";
import ScopeIdentity from "../../../components/ScopeIdentity.astro";
import ScopeTransitions from "../../../components/ScopeTransitions.astro";
import StructuralMethods from "../../../components/StructuralMethods.astro";
import { buildScopeReference } from "../../../scope-reference.ts";

export const model = buildScopeReference("country", {
  description:
    "Country scope represents a game object that Stellaris evaluates as a country, including an empire acting in its own script context.",
  commonEntries:
    "Common entry points include country and observer event bodies, country-scoped on-actions and callbacks, and scope links from owned game objects. These are examples, not an exhaustive list of game paths.",
});

<ScopeIdentity model={model} />

## Common entry points

{model.prose.commonEntries}

## Event kinds whose bodies run in this scope

<EventKinds rows={model.eventKinds} />

## Ordinary effects

<ScopeEffects universal={model.universalEffects} scopeSpecific={model.scopeEffects} />

## Structural and control-flow methods

<StructuralMethods rows={model.structuralMethods} />

## Event-fire methods

<EventFireMethods rows={model.eventFireMethods} />

## Outgoing scope transitions

<ScopeTransitions rows={model.transitions} />
