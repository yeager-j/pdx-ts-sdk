---
import type { ScriptMethodRow } from "../scope-reference.ts";
import EffectReferenceTable from "./EffectReferenceTable.astro";

interface Props {
  rows: readonly ScriptMethodRow[];
}

const { rows } = Astro.props;
---

<p>
  These handwritten SDK methods record branching, iteration, saved targets, and other script
  structure. They are listed separately from ordinary generated effects.
</p>

<EffectReferenceTable rows={rows} caption="Structural and control-flow methods" />
