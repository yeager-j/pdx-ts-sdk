// Generated by tools/codegen. Do not edit.
// Source: cwtools-stellaris-config @ 251fe1189b4e
// From: value sets referenced across the rule files

declare const valueSetBrand: unique symbol;

/**
 * A name belonging to one of the game's value sets.
 * The brand is optional so a raw string still assigns — vanilla and other
 * mods' names have to keep working — but two different sets are not
 * interchangeable once a name has been declared as belonging to one.
 */
export type ValueSetMember<T extends string> = string & {
  readonly [valueSetBrand]?: T;
};

/**
 * Builds a lookup of names belonging to a single value set.
 * The keys are the names themselves, so `flags.heard_the_hum` autocompletes
 * and a typo is a compile error rather than a condition that never fires.
 */
function declare<T extends string>() {
  return <const Names extends readonly string[]>(
    ...names: Names
  ): { readonly [K in Names[number]]: ValueSetMember<T> } => {
    const entries = names.map((name) => [name, name] as const);
    return Object.fromEntries(entries) as {
      readonly [K in Names[number]]: ValueSetMember<T>;
    };
  };
}

/** A name belonging to `value[agreement_flag]`. */
export type AgreementFlag = ValueSetMember<"agreement_flag">;

/**
 * Declares agreement_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const agreementFlags = declare<"agreement_flag">();

/** A name belonging to `value[ambient_object_flag]`. */
export type AmbientObjectFlag = ValueSetMember<"ambient_object_flag">;

/**
 * Declares ambient_object_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const ambientObjectFlags = declare<"ambient_object_flag">();

/** A name belonging to `value[archaeology_flag]`. */
export type ArchaeologyFlag = ValueSetMember<"archaeology_flag">;

/**
 * Declares archaeology_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const archaeologyFlags = declare<"archaeology_flag">();

/** A name belonging to `value[army_flag]`. */
export type ArmyFlag = ValueSetMember<"army_flag">;

/**
 * Declares army_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const armyFlags = declare<"army_flag">();

/** A name belonging to `value[astral_rift_flag]`. */
export type AstralRiftFlag = ValueSetMember<"astral_rift_flag">;

/**
 * Declares astral_rift_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const astralRiftFlags = declare<"astral_rift_flag">();

/** A name belonging to `value[building_set]`. */
export type BuildingSet = ValueSetMember<"building_set">;

/**
 * Declares building_set names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const buildingSets = declare<"building_set">();

/** A name belonging to `value[carrier_flag]`. */
export type CarrierFlag = ValueSetMember<"carrier_flag">;

/**
 * Declares carrier_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const carrierFlags = declare<"carrier_flag">();

/** A name belonging to `value[cluster_id]`. */
export type ClusterId = ValueSetMember<"cluster_id">;

/**
 * Declares cluster_id names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const clusterIds = declare<"cluster_id">();

/** A name belonging to `value[construction_type]`. */
export type ConstructionType = ValueSetMember<"construction_type">;

/**
 * Declares construction_type names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const constructionTypes = declare<"construction_type">();

/** A name belonging to `value[country_flag]`. */
export type CountryFlag = ValueSetMember<"country_flag">;

/**
 * Declares country_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const countryFlags = declare<"country_flag">();

/** A name belonging to `value[deposit_flag]`. */
export type DepositFlag = ValueSetMember<"deposit_flag">;

/**
 * Declares deposit_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const depositFlags = declare<"deposit_flag">();

/** A name belonging to `value[design_flag]`. */
export type DesignFlag = ValueSetMember<"design_flag">;

/**
 * Declares design_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const designFlags = declare<"design_flag">();

/** A name belonging to `value[espionage_asset_flag]`. */
export type EspionageAssetFlag = ValueSetMember<"espionage_asset_flag">;

/**
 * Declares espionage_asset_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const espionageAssetFlags = declare<"espionage_asset_flag">();

/** A name belonging to `value[espionage_operation_flag]`. */
export type EspionageOperationFlag = ValueSetMember<"espionage_operation_flag">;

/**
 * Declares espionage_operation_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const espionageOperationFlags = declare<"espionage_operation_flag">();

/** A name belonging to `value[exiled_leader]`. */
export type ExiledLeader = ValueSetMember<"exiled_leader">;

/**
 * Declares exiled_leader names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const exiledLeaders = declare<"exiled_leader">();

/** A name belonging to `value[federation_flag]`. */
export type FederationFlag = ValueSetMember<"federation_flag">;

/**
 * Declares federation_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const federationFlags = declare<"federation_flag">();

/** A name belonging to `value[first_contact_flag]`. */
export type FirstContactFlag = ValueSetMember<"first_contact_flag">;

/**
 * Declares first_contact_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const firstContactFlags = declare<"first_contact_flag">();

/** A name belonging to `value[fleet_automation_setting]`. */
export type FleetAutomationSetting = ValueSetMember<"fleet_automation_setting">;

/**
 * Declares fleet_automation_setting names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const fleetAutomationSettings = declare<"fleet_automation_setting">();

/** A name belonging to `value[fleet_flag]`. */
export type FleetFlag = ValueSetMember<"fleet_flag">;

/**
 * Declares fleet_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const fleetFlags = declare<"fleet_flag">();

/** A name belonging to `value[global_event_target]`. */
export type GlobalEventTarget = ValueSetMember<"global_event_target">;

/**
 * Declares global_event_target names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const globalEventTargets = declare<"global_event_target">();

/** A name belonging to `value[global_flag]`. */
export type GlobalFlag = ValueSetMember<"global_flag">;

/**
 * Declares global_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const globalFlags = declare<"global_flag">();

/** A name belonging to `value[leader_flag]`. */
export type LeaderFlag = ValueSetMember<"leader_flag">;

/**
 * Declares leader_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const leaderFlags = declare<"leader_flag">();

/** A name belonging to `value[megastructure_flag]`. */
export type MegastructureFlag = ValueSetMember<"megastructure_flag">;

/**
 * Declares megastructure_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const megastructureFlags = declare<"megastructure_flag">();

/** A name belonging to `value[mission_flag]`. */
export type MissionFlag = ValueSetMember<"mission_flag">;

/**
 * Declares mission_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const missionFlags = declare<"mission_flag">();

/** A name belonging to `value[planet_flag]`. */
export type PlanetFlag = ValueSetMember<"planet_flag">;

/**
 * Declares planet_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const planetFlags = declare<"planet_flag">();

/** A name belonging to `value[point_of_interest]`. */
export type PointOfInterest = ValueSetMember<"point_of_interest">;

/**
 * Declares point_of_interest names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const pointOfInterests = declare<"point_of_interest">();

/** A name belonging to `value[pop_faction_flag]`. */
export type PopFactionFlag = ValueSetMember<"pop_faction_flag">;

/**
 * Declares pop_faction_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const popFactionFlags = declare<"pop_faction_flag">();

/** A name belonging to `value[pop_group_flag]`. */
export type PopGroupFlag = ValueSetMember<"pop_group_flag">;

/**
 * Declares pop_group_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const popGroupFlags = declare<"pop_group_flag">();

/** A name belonging to `value[relation_flag]`. */
export type RelationFlag = ValueSetMember<"relation_flag">;

/**
 * Declares relation_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const relationFlags = declare<"relation_flag">();

/** A name belonging to `value[saved_leader]`. */
export type SavedLeader = ValueSetMember<"saved_leader">;

/**
 * Declares saved_leader names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const savedLeaders = declare<"saved_leader">();

/** A name belonging to `value[sector_flag]`. */
export type SectorFlag = ValueSetMember<"sector_flag">;

/**
 * Declares sector_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const sectorFlags = declare<"sector_flag">();

/** A name belonging to `value[ship_flag]`. */
export type ShipFlag = ValueSetMember<"ship_flag">;

/**
 * Declares ship_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const shipFlags = declare<"ship_flag">();

/** A name belonging to `value[situation_flag]`. */
export type SituationFlag = ValueSetMember<"situation_flag">;

/**
 * Declares situation_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const situationFlags = declare<"situation_flag">();

/** A name belonging to `value[species_flag]`. */
export type SpeciesFlag = ValueSetMember<"species_flag">;

/**
 * Declares species_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const speciesFlags = declare<"species_flag">();

/** A name belonging to `value[spynetwork_flag]`. */
export type SpynetworkFlag = ValueSetMember<"spynetwork_flag">;

/**
 * Declares spynetwork_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const spynetworkFlags = declare<"spynetwork_flag">();

/** A name belonging to `value[star_flag]`. */
export type StarFlag = ValueSetMember<"star_flag">;

/**
 * Declares star_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const starFlags = declare<"star_flag">();

/** A name belonging to `value[starbase_flag]`. */
export type StarbaseFlag = ValueSetMember<"starbase_flag">;

/**
 * Declares starbase_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const starbaseFlags = declare<"starbase_flag">();

/** A name belonging to `value[storm_flag]`. */
export type StormFlag = ValueSetMember<"storm_flag">;

/**
 * Declares storm_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const stormFlags = declare<"storm_flag">();

/** A name belonging to `value[timeline_event_id]`. */
export type TimelineEventId = ValueSetMember<"timeline_event_id">;

/**
 * Declares timeline_event_id names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const timelineEventIds = declare<"timeline_event_id">();

/** A name belonging to `value[variable]`. */
export type Variable = ValueSetMember<"variable">;

/**
 * Declares variable names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const variables = declare<"variable">();

/** A name belonging to `value[war_flag]`. */
export type WarFlag = ValueSetMember<"war_flag">;

/**
 * Declares war_flag names, so they autocomplete and cannot be
 * confused with a name from another set.
 */
export const warFlags = declare<"war_flag">();
