// Generated by tools/codegen. Do not edit.
// Source: cwtools-stellaris-config @ 251fe1189b4e
// From: events/events.cwt

import type { DefinedEvent, EventDef } from "../events.ts";
import { GeneratedContentMethods } from "./content-registry.ts";
import type { EventKindKey } from "./events.ts";
import type { ScopeName } from "./scopes.ts";

/**
 * The `defineXEvent` methods, one per scoped event kind. `Mod` extends
 * this the same way it extends `GeneratedContentMethods`, which this
 * class chains to so the two generated surfaces share one base chain.
 */
export abstract class GeneratedEventMethods<
  const P extends string,
> extends GeneratedContentMethods<P> {
  protected abstract defineEventOf<S extends ScopeName, From extends ScopeName | undefined>(
    kind: EventKindKey,
    scope: S,
    def: EventDef<S, From>
  ): DefinedEvent<S, From>;

  /**
   * Defines a agreement event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineAgreementEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"agreement", From>
  ): DefinedEvent<"agreement", From> {
    return this.defineEventOf("agreement_event", "agreement", def);
  }

  /**
   * Defines a astral rift event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineAstralRiftEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"astral_rift", From>
  ): DefinedEvent<"astral_rift", From> {
    return this.defineEventOf("astral_rift_event", "astral_rift", def);
  }

  /**
   * Defines a bypass event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineBypassEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"bypass", From>
  ): DefinedEvent<"bypass", From> {
    return this.defineEventOf("bypass_event", "bypass", def);
  }

  /**
   * Defines a carrier event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineCarrierEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"carrier", From>
  ): DefinedEvent<"carrier", From> {
    return this.defineEventOf("carrier_event", "carrier", def);
  }

  /**
   * Defines a colony event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineColonyEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"colony", From>
  ): DefinedEvent<"colony", From> {
    return this.defineEventOf("colony_event", "colony", def);
  }

  /**
   * Defines a cosmic storm event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineCosmicStormEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"storm", From>
  ): DefinedEvent<"storm", From> {
    return this.defineEventOf("cosmic_storm_event", "storm", def);
  }

  /**
   * Defines a cosmic storm influence field event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineCosmicStormInfluenceFieldEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"cosmic_storm_influence_field", From>
  ): DefinedEvent<"cosmic_storm_influence_field", From> {
    return this.defineEventOf(
      "cosmic_storm_influence_field_event",
      "cosmic_storm_influence_field",
      def
    );
  }

  /**
   * Defines a country event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineCountryEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"country", From>
  ): DefinedEvent<"country", From> {
    return this.defineEventOf("country_event", "country", def);
  }

  /**
   * Defines a espionage operation event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineEspionageOperationEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"espionage_operation", From>
  ): DefinedEvent<"espionage_operation", From> {
    return this.defineEventOf("espionage_operation_event", "espionage_operation", def);
  }

  /**
   * Defines a first contact event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineFirstContactEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"first_contact", From>
  ): DefinedEvent<"first_contact", From> {
    return this.defineEventOf("first_contact_event", "first_contact", def);
  }

  /**
   * Defines a fleet event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineFleetEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"fleet", From>
  ): DefinedEvent<"fleet", From> {
    return this.defineEventOf("fleet_event", "fleet", def);
  }

  /**
   * Defines a leader event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineLeaderEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"leader", From>
  ): DefinedEvent<"leader", From> {
    return this.defineEventOf("leader_event", "leader", def);
  }

  /**
   * Defines a observer event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineObserverEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"country", From>
  ): DefinedEvent<"country", From> {
    return this.defineEventOf("observer_event", "country", def);
  }

  /**
   * Defines a planet event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  definePlanetEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"planet", From>
  ): DefinedEvent<"planet", From> {
    return this.defineEventOf("planet_event", "planet", def);
  }

  /**
   * Defines a pop faction event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  definePopFactionEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"pop_faction", From>
  ): DefinedEvent<"pop_faction", From> {
    return this.defineEventOf("pop_faction_event", "pop_faction", def);
  }

  /**
   * Defines a pop group event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  definePopGroupEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"pop_group", From>
  ): DefinedEvent<"pop_group", From> {
    return this.defineEventOf("pop_group_event", "pop_group", def);
  }

  /**
   * Defines a ship event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineShipEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"ship", From>
  ): DefinedEvent<"ship", From> {
    return this.defineEventOf("ship_event", "ship", def);
  }

  /**
   * Defines a situation event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineSituationEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"situation", From>
  ): DefinedEvent<"situation", From> {
    return this.defineEventOf("situation_event", "situation", def);
  }

  /**
   * Defines a starbase event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineStarbaseEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"starbase", From>
  ): DefinedEvent<"starbase", From> {
    return this.defineEventOf("starbase_event", "starbase", def);
  }

  /**
   * Defines a system event in this mod's namespace; the full id is
   * `${prefix}.${def.id}`. Title/desc/option localization rides along, and the
   * event's closures record eagerly, at the define site.
   */
  defineSystemEvent<From extends ScopeName | undefined = undefined>(
    def: EventDef<"system", From>
  ): DefinedEvent<"system", From> {
    return this.defineEventOf("system_event", "system", def);
  }
}
